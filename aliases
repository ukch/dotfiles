# vim: set ft=sh

alias rm="echo Please use trash or /bin/rm && false"
alias gvir="gvim --remote"
alias pt="pytest --emoji"
alias vim="nvim -n" # ignore swap files for terminal vim sessions

export WERKZEUG_DEBUG_PIN=off
export PIP_REQUIRE_VIRTUALENV=1
export ACK_PAGER=less
export apt_pref=apt
export apt_upgr=upgrade
export NVIM_LISTEN_ADDRESS=/tmp/nvimsocket
export BUILDER=pbuilder  # for git-buildpackage

export PATH=./node_modules/.bin:$PATH:$HOME/opt/android-sdk-linux/tools:$HOME/opt/android-sdk-linux/platform-tools
export ANDROID_HOME=/opt/android-sdk-linux

###-tns-completion-start-###
if [ -f /home/joel/.tnsrc ]; then
    source /home/joel/.tnsrc
fi
###-tns-completion-end-###

export DEBEMAIL="joel@kazbak.co.uk"
export DEBFULLNAME="Joel Cross"
